import React from 'react';
import { TextMessage } from './error-message.styled';

const ErrorMessage = () => (
  <TextMessage>Something went wrong. Please try again later</TextMessage>
);

export default ErrorMessage;
